export enum Actions {
  REVOKE = 'revoke',
  PROMOTE = 'promote',
}

export enum Role {
  MODERATOR = 'moderator',
}

export enum filterApiRole {
  NOT_MODERATOR = '!moderator',
  MODERATOR = 'moderator',
}
